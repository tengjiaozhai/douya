# Repository Rules

## Rule: README Sync Is Mandatory

1. 每次完成任何代码变更（包括 `src/`、`apps/`、`libs/`、`docs/`、配置文件、脚本）后，必须在仓库根目录 `README.md` 同步更新本次实现结果。
2. `README.md` 更新至少包含：
   - 变更摘要（做了什么）
   - 行为变化（对调用方/运行方式的影响）
   - 必要的配置变化（新增/删除配置项）
3. 如果本次改动没有新增功能，也必须在 `README.md` 的更新日志中补充最小记录，说明“重构/清理/修复”内容。
4. 该规则对所有会话生效（包括新会话）。
